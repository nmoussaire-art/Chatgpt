<?xml version="1.0" encoding="utf-8"?>
<!--
  Battery history is personal behavioural data, so none of it is eligible for cloud backup or
  device-to-device transfer. Combined with android:allowBackup="false", the observation database
  exists on exactly one device and leaves it only if the user exports it deliberately.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
